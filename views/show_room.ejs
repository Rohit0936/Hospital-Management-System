<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Show Rooms</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
  <link rel="stylesheet" href="/sidebar.css" />
  <link rel="stylesheet" href="/show_doc.css" />
  <script type="text/javascript" src="searchdoc.js"></script>
  <style>
    .page-wrapper {
      margin-left: 250px;
      padding: 20px;
    }

    @media (max-width: 768px) {
      .page-wrapper {
        margin-left: 0;
      }
    }

    /* Internal CSS for header logo */
    .header-container {
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1.5rem;
    }

    .header-logo {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background-color: #e74c3c;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 24px;
      box-shadow: 0 0 8px rgba(0,0,0,0.1);
      margin-right: 10px;
    }
  </style>
</head>
<body>
  <%- include('rec_dashbord.ejs') %>

  <div class="page-wrapper">
    <div class="container py-4">
      <nav class="navbar navbar-light bg-light">
        <div class="container-fluid">
          <form class=>
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" onkeyup="room(this.value)">
          </form>
          <nav >
            <form class="container-fluid justify-content-start">
              <a href="/show_room?s=General" class="btn btn-sm btn-outline-secondary" >General Room</a>
              <a href="/show_room?s=ICU" class="btn btn-sm btn-outline-secondary" >ICU Room</a>
              <a href="/show_room?s=AC" class="btn btn-sm btn-outline-secondary" >AC Room</a>
              <a href="/show_room?s=true" class="btn btn-sm btn-outline-secondary" >Avaiable</a>
              <a href="/show_room?s=false" class="btn btn-sm btn-outline-secondary" >Not Avaiable</a>
            </form>
          </nav>
        </div>
      </nav>
      <br>
      <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 g-4" id="maindiv">
        <%
          if(data.length==0)
          {
        %>
          <h1 style="width:100%; text-align: center;">Room Not Avaiable 😑</h1>
        <%
          }
          else
          {
              data.forEach((item,index)=>{
         %>
         <div class="col" id="div1">
          <div class="card h-100 shadow room-card text-center" id="div2">
            <div class="card-body d-flex flex-column align-items-center p-4" id="div3">
              <div class="room-icon mb-3" id="div4">
                <i class="fas fa-bed"></i>
              </div>
              <%
              if(item.Room_type==="General")
              {
              %>
              <h5 class="fw-bold text-dark mb-1">Bed no: <%=item.room_no%></h5>
              <%
              }
              else
              {
              %>
              <h5 class="fw-bold text-dark mb-1">Room no: <%=item.room_no%></h5>
              <%
              }
              %>
              
              <p class="text-muted mb-2"><i class="fas fa-hospital me-1 text-danger"></i><%=item.Room_type%></p>
              <p class="mb-3"><i class="fas fa-money-bill me-1 text-success"></i> ₹<%=item.Room_Charges%>/day</p>
              <%
                 if(item.room_status==="true")
                 {
              %>
              <h6><label >Status:</label><label for="" style="color: green;">Avaiable</label></h6>
              <%
                 }
                 else{

              %>
              <h6><label >Status:</label>   <label for="" style="color: red;">Not Avaiable</label></h6>
              <%
                 }
              %>
             
            </div>
            <div class="card-footer bg-white d-flex justify-content-center gap-2" id="div5">
              <a href="/updateroom?rid=<%=item.room_id%>" class="btn btn-outline-primary btn-sm px-3"><i class="fas fa-pen me-1"></i>Edit</a>
              <a href="/deleteroom?rid=<%=item.room_id%>" class="btn btn-outline-danger btn-sm px-3"><i class="fas fa-trash me-1"></i>Delete</a>
            </div>
          </div>
        </div> 
         <%
              })
          }
        %>
      </div>
    </div>
  </div>

</div> <!-- Closes .main-content from rec_dashbord -->
</body>
</html>