<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Doctor Dashboard</title>

  <!-- Bootstrap & FontAwesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
  <script type="text/javascript" src="searchdoc.js"></script>

  <style>
    body {
  background: linear-gradient(to right, #2980b9, #6dd5fa); /* smoother blue blend */
  font-family: 'Segoe UI', sans-serif;
  transition: background 0.6s ease-in-out, color 0.4s ease-in-out;
}


    .page-wrapper {
      margin-left: 250px;
      padding: 30px;
      transition: margin-left 0.3s ease;
    }

    @media (max-width: 768px) {
      .page-wrapper {
        margin-left: 0;
      }
    }

    .doctor-card-new {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      position: relative;
      overflow: hidden;
      backdrop-filter: blur(12px);
    }

    .doctor-card-new:hover {
      transform: translateY(-10px) scale(1.03);
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
    }

    .doctor-card-new .card-content {
      padding: 30px;
      position: relative;
      z-index: 1;
    }

    .header-container {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 15px;
      margin-bottom: 1.5rem;
    }

    .header-logo {
      font-size: 2.5rem;
      color: #e74c3c;
      margin-right: 10px;
    }

    .text-dark {
      color: #1e272e !important;
      font-weight: 700;
      letter-spacing: 0.5px;
    }

    .card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 25px;
      margin-top: 40px;
    }

    .feature-card {
      background: rgba(255, 255, 255, 0.15);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 20px;
      padding: 25px;
      text-align: center;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      position: relative;
      backdrop-filter: blur(8px);
    }

    .feature-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
    }

    .feature-card i {
      font-size: 3rem;
      margin-bottom: 15px;
    }

    .feature-card h4 {
      color: #2d3436;
      font-weight: 600;
      font-size: 1.3rem;
      margin-bottom: 12px;
    }

    .feature-card p {
      color: #f5f6fa;
    }

    .feature-card:nth-child(1) i { color: #00a8ff; }
    .feature-card:nth-child(2) i { color: #9c88ff; }
    .feature-card:nth-child(3) i { color: #e84118; }

    .gradient-divider {
      height: 4px;
      width: 100px;
      background: linear-gradient(90deg, #00a8ff, #0097e6, #8c7ae6);
      background-size: 200% 100%;
      border-radius: 3px;
      animation: gradientShift 4s ease-in-out infinite;
    }

    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      100% { background-position: 100% 50%; }
    }

    .doctor-avatar {
      width: 100px;
      height: 100px;
      border: 3px solid #e74c3c;
      border-radius: 50%;
      overflow: hidden;
      box-shadow: 0 0 8px rgba(0,0,0,0.1);
    }

    .doctor-avatar img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .patient-card {
      border-radius: 12px;
      transition: transform 0.2s ease;
    }

    .patient-card:hover {
      transform: translateY(-5px);
    }

    .patient-icon {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background-color: #e74c3c;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 24px;
      box-shadow: 0 0 8px rgba(0,0,0,0.1);
    }

    .truncate-text {
      display: -webkit-box;
       
      -webkit-box-orient: vertical;
      overflow: hidden;
      text-overflow: ellipsis;
    }
  </style>
</head>
<body>


  <!-- Include reusable sidebar -->
  <%- include("doctor_slider.ejs") %>

  <div class="page-wrapper">
    <div class="container py-5">
      <!-- Welcome Message Card -->
      <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8">
          <div class="card doctor-card-new text-center">
            <div class="card-content">
              <div class="header-container">
                <i class="fas fa-user-md header-logo"></i>
                <h2 class="text-dark mb-0">Welcome Doctor</h2>
              </div>
              <div class="gradient-divider mx-auto mb-4"></div>
              <h3>Dr. <%= data.name %></h3>
            </div>
          </div>
        </div>
      </div>

      <!-- Feature Cards -->
      <div class="card-grid mt-5">
        <div class="feature-card">
          <i class="fas fa-notes-medical"></i>
          <h4>Medical Records</h4>
          <p>Access and manage patient health records efficiently.</p>
        </div>
        <div class="feature-card">
          <i class="fas fa-calendar-check"></i>
          <h4>Appointments</h4>
          <p>View and update your scheduled appointments.</p>
        </div>
        <div class="feature-card">
          <i class="fas fa-stethoscope"></i>
          <h4>Diagnosis Tools</h4>
          <p>Use diagnostic tools to support your consultations.</p>
        </div>
      </div>
    </div>
  </div>

</body>
</html>
