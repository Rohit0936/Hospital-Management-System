<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Doctor Panel</title>

  <!-- Bootstrap & Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet" />

  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
    }

    .sidebar {
      min-height: 100vh;
      width: 260px;
      /* background-color: #2856a0; */
      color: white;
      padding-top: 30px;
      position: fixed;
      box-shadow: 4px 0 12px rgba(0, 0, 0, 0.2);
      /* transition: transform 0.3s ease-in-out; */
      z-index: 1000;
      background: linear-gradient(to right, #2c3e50, #3498db);
  transition: background 0.6s ease-in-out, color 0.4s ease-in-out;

    }

    .sidebar img {
      display: block;
      margin: 0 auto;
      width: 100px;
      height: 100px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid #facc15;
    }

    .doctor-name {
      text-align: center;
      font-weight: bold;
      margin-top: 10px;
      font-size: 16px;
      color: #facc15;
    }

    .sidebar .nav-link {
      color: #cbd5e1;
      padding: 12px 20px;
      transition: all 0.3s;
    }

    .sidebar .nav-link:hover,
    .sidebar .nav-link.active {
      background-color: #334155;
      color: #facc15;
    }

    @media (max-width: 768px) {
      .sidebar {
        transform: translateX(-100%);
        width: 100%;
      }

      .sidebar.show {
        transform: translateX(0%);
      }
    }
  </style>
</head>
<body>

<!-- Sidebar -->
<div id="sidebar" class="sidebar">
  <img src="<%= data.image %>" alt="Doctor" />
  <div class="doctor-name">Dr. <%= data.name %></div>

  <ul class="nav nav-pills flex-column my-4">
    <li class="nav-item">
      <a href="/" class="nav-link text-white">
        <i class="fas fa-home me-2"></i> Dashboard
      </a>
    </li>
    <li>
      <a href="/showdocpatient?s=n" class="nav-link text-white">
        <i class="fas fa-user-injured me-2"></i> Patients
      </a>
    </li>
    <li>
      <a href="/appointments" class="nav-link text-white">
        <i class="fas fa-calendar-check me-2"></i> Appointments
      </a>
    </li>
    <li>
      <a href="/profile" class="nav-link text-white">
        <i class="fas fa-user-circle me-2"></i> Profile
      </a>
    </li>
    <li>
      <a href="/logout" class="nav-link text-white">
        <i class="fas fa-sign-out-alt me-2"></i> Logout
      </a>
    </li>
  </ul>
</div>

<!-- Mobile Toggle Button (works both open/close) -->
<button class="btn btn-primary d-md-none position-fixed" style="top: 10px; left: 10px; z-index: 1100;"
  onclick="document.getElementById('sidebar').classList.toggle('show')">
  <i class="fas fa-bars"></i>
</button>

</body>
</html>
