<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Receptionist Panel - Healthcare</title>

  <!-- Bootstrap & FontAwesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />

  <!-- Custom Internal CSS -->
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      background: linear-gradient(135deg, #e3f2fd 0%, #ffffff 100%);
      min-height: 100vh;
      background-attachment: fixed;
      transition: background 0.3s ease, color 0.3s ease;
    }

    .sidebar {
      height: 100vh;
      width: 250px;
      position: fixed;
      background-color: #1d1b1b;
      color: #fff;
      padding-top: 20px;
      overflow-y: auto;
      text-align: center;
      transition: transform 0.3s ease;
      z-index: 1000;
    }

    .sidebar img {
      width: 120px;
      height: 120px;
      object-fit: cover;
      border-radius: 50%;
      border: 4px solid #fff;
    }

    .sidebar h6 {
      margin-top: 10px;
      color: #ffc107;
    }

    .sidebar h4 {
      color: #fff;
      margin: 20px 0;
    }

    .sidebar .nav-link {
      color: #ecf0f1;
      padding: 10px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .sidebar .nav-link:hover,
    .sidebar .nav-link.active {
      background-color: #34495e;
      color: #ffc107;
    }

    .submenu {
      max-height: 0;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      padding-left: 30px;
      transition: max-height 0.3s ease;
      background-color: #2c2a2a;
    }

    .submenu a {
      font-size: 14px;
      color: #ecf0f1;
      padding: 8px 0;
      border-left: 2px solid #ffc107;
      margin: 5px 0;
      transition: color 0.2s ease, padding-left 0.2s ease;
    }

    .submenu a:hover {
      color: #ffc107;
      padding-left: 10px;
    }

    input[type="checkbox"] {
      display: none;
    }

    input[type="checkbox"]:checked + label + .submenu {
      max-height: 200px;
    }

    /* Mobile Sidebar */
    .toggle-btn {
      display: none;
      position: fixed;
      top: 15px;
      left: 15px;
      font-size: 24px;
      z-index: 1100;
      color: #1d1b1b;
      background-color: #fff;
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
    }

    .sidebar.active {
      transform: translateX(0);
    }

    @media (max-width: 768px) {
      .sidebar {
        width: 100%;
        position: fixed;
        transform: translateX(-100%);
      }

      .toggle-btn {
        display: block;
      }
    }

    /* Dark mode */
    .dark-mode {
      background: #121212 !important;
      color: #f5f5f5 !important;
      
    }

    .dark-mode .sidebar {
      background-color: #000 !important;
    }

    .dark-mode .sidebar .nav-link {
      color: #ccc;
    }

    .dark-mode .sidebar .nav-link:hover {
      background-color: #222;
      color: #ffc107;
    }

    .dark-switch {
      font-size: 20px;
      cursor: pointer;
      color: #ecf0f1;
      margin: 15px auto;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px; /* Space between icon and text */
    }

    .dark-switch span {
      font-size: 14px;
      color: #ecf0f1;
    }

    .dark-mode .dark-switch span {
      color: #ccc;
    }
  </style>
</head>

<body>


  <div class="toggle-btn" onclick="Sidebar()">
    <i class="fas fa-bars"></i>
  </div>
  
  <!-- Sidebar -->
<div class="sidebar" id="sidebar">
    <img src="<%=data[1]%>" alt="Receptionist">
    <h6>Dr.<%=data[0]%></h6>
    <h4><i class="fas fa-hospital"></i> Doctor Panel</h4>
  
    <ul class="nav flex-column text-start px-3">
      <li class="nav-item">
        <a class="nav-link active" href="/receptionistdashboard"><i class="fas fa-tachometer-alt me-2"></i> Dashboard</a>
      </li>
  
      <!-- Show Patients only -->
      <li class="nav-item">
        <a class="nav-link" href="/showdocpatient"><i class="fas fa-users me-2"></i> 📋 Show Patients</a>
      </li>
  
      <li class="nav-item">
        <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt me-2"></i> Logout</a>
      </li>
    </ul>
  
    <!-- Dark Mode Switch -->
    <div class="dark-switch" onclick="DarkMode()">
      <i class="fas fa-moon"></i>
      <span>Dark Mode</span>
    </div>
  </div>
  
  <!-- JS -->
  <script>
    function Sidebar() {
      document.getElementById("sidebar").classList.toggle("active");
    }

    function DarkMode() {
      document.body.classList.toggle("dark-mode");
    }
  </script>
  
</body>
</html>